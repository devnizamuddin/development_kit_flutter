import 'package:flutter/material.dart';

class MsGradient {
  /*
   * ┏==================================================================================================┓
   * ┃                                      Initial Gradient                                            ┃
   * ┗==================================================================================================┛
   */
  static const chartBarGradient = LinearGradient(
    colors: [Colors.indigo, Colors.cyan],
    begin: Alignment.bottomCenter,
    end: Alignment.topCenter,
  );

  static const warmSunsetGradient = LinearGradient(
    colors: [Color(0xFFFF6E7F), Color(0xFFBFE9FF)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const oceanBlueGradient = LinearGradient(
    colors: [Color(0xFF36D1DC), Color(0xFF5B86E5)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const greenFreshGradient = LinearGradient(
    colors: [Color(0xFF11998E), Color(0xFF38EF7D)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

/*
 * ┏==================================================================================================┓
 * ┃                                      More Beautiful Gradient                                     ┃
 * ┗==================================================================================================┛
 */

  static const sunriseGradient = LinearGradient(
    colors: [Color(0xFFFF512F), Color(0xFFDD2476)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const lavenderSkyGradient = LinearGradient(
    colors: [Color(0xFFB993D6), Color(0xFF8CA6DB)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const peachyGradient = LinearGradient(
    colors: [Color(0xFFFFE29F), Color(0xFFFF7E5F)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const aquaMarineGradient = LinearGradient(
    colors: [Color(0xFF1A2980), Color(0xFF26D0CE)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const fireAndIceGradient = LinearGradient(
    colors: [Color(0xFFED4264), Color(0xFFFFEDBC)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const violetSkyGradient = LinearGradient(
    colors: [Color(0xFFDA22FF), Color(0xFF9733EE)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const coralReefGradient = LinearGradient(
    colors: [Color(0xFFFF9A8B), Color(0xFFFF6A88)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const mintyFreshGradient = LinearGradient(
    colors: [Color(0xFF43CBFF), Color(0xFF9708CC)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const goldenHourGradient = LinearGradient(
    colors: [Color(0xFFFFD194), Color(0xFF70E1F5)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const deepOceanGradient = LinearGradient(
    colors: [Color(0xFF2BC0E4), Color(0xFFEAECC6)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  /*
   * ┏==================================================================================================┓
   * ┃                                      Ultra Modern Gradient                                       ┃
   * ┗==================================================================================================┛
   */
  static const neonSunsetGradient = LinearGradient(
    colors: [Color(0xFFFF3CAC), Color(0xFF784BA0)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const electricBlueGradient = LinearGradient(
    colors: [Color(0xFF00F260), Color(0xFF0575E6)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const cosmicPurpleGradient = LinearGradient(
    colors: [Color(0xFF833AB4), Color(0xFFFFAF7B)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const flamingoPinkGradient = LinearGradient(
    colors: [Color(0xFFFF6B6B), Color(0xFFF7C59F)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const turquoiseDreamGradient = LinearGradient(
    colors: [Color(0xFF43E97B), Color(0xFF38F9D7)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const sunsetOrangeGradient = LinearGradient(
    colors: [Color(0xFFFF8008), Color(0xFFFFC837)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const violetWaveGradient = LinearGradient(
    colors: [Color(0xFF7F00FF), Color(0xFFE100FF)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const mintyAquaGradient = LinearGradient(
    colors: [Color(0xFF00F5A0), Color(0xFF00D9F5)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const candyFlossGradient = LinearGradient(
    colors: [Color(0xFFFFA6C1), Color(0xFFFFD3A5)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const auroraGreenGradient = LinearGradient(
    colors: [Color(0xFF00FF9C), Color(0xFF00C9FF)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  /*
   * ┏==================================================================================================┓
   * ┃                                      Dark Gradient                                               ┃
   * ┗==================================================================================================┛
   */

  static const midnightBlueGradient = LinearGradient(
    colors: [Color(0xFF0F2027), Color(0xFF203A43), Color(0xFF2C5364)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const deepSpaceGradient = LinearGradient(
    colors: [Color(0xFF232526), Color(0xFF414345)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const darkPurpleGradient = LinearGradient(
    colors: [Color(0xFF2C3E50), Color(0xFF4CA1AF)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const blackPearlGradient = LinearGradient(
    colors: [Color(0xFF141E30), Color(0xFF243B55)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const obsidianGradient = LinearGradient(
    colors: [Color(0xFF0F0C29), Color(0xFF302B63), Color(0xFF24243E)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const deepEmeraldGradient = LinearGradient(
    colors: [Color(0xFF134E5E), Color(0xFF71B280)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const galaxyGradient = LinearGradient(
    colors: [Color(0xFF20002C), Color(0xFFcbb4d4)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const charcoalGradient = LinearGradient(
    colors: [Color(0xFF333333), Color(0xFFDD1818)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const smokyAshGradient = LinearGradient(
    colors: [Color(0xFF0D324D), Color(0xFF7F5A83)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );

  static const deepSeaGradient = LinearGradient(
    colors: [Color(0xFF000046), Color(0xFF1CB5E0)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );
}
