part of '../theme.dart';

class MsIconThemeData extends IconThemeData {
  const MsIconThemeData()
      : super(
          color: MsColor.primary, // Default icon color for light theme
          size: 24.0, // Default icon size
          opacity: 1.0, // Default icon opacity
        );
}
